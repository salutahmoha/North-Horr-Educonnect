import styled from 'styled-components';

export const FullReportContainer = styled.div`
    max-width: 800px;
    margin: 5rem auto;
    padding: 1rem;
    background-color: var(--color-form);
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    color: 000;
`;

export const TitleStyled = styled.h1`
    font-size: 2rem;
    color: #000;
    text-align: start;
`;

export const ImageStyled = styled.img`
    width: 100%;
    height: auto;
    margin: 1rem 0;
    border-radius: 8px;
`;

export const BodyStyled = styled.div`
    font-size: 1rem;
    color: #000;
    line-height: 1.6;
`;
